import React from 'react';

class Header extends React.Component {
  render() {
    return (
      <header>
        <h2>Clock</h2>
        <button>Modo</button>
      </header>
    );
  }
}

export default Header;
